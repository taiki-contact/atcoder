#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(int argc, char const *argv[])
{
	int N;
	cin >> N;
	std::vector<int> x(N+1);
	std::vector<int> y(N+1);
	ll m = 1;

	for (int i = 1; i <= N; ++i)
	{
		cin >> x[i] >> y[i];
		m *= i
	}


	}


	return 0;
}